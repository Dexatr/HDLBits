module top_module ( input clk, input d, output q );

    wire wire1, wire2;
    
    my_dff instance1 ( .clk(clk), .d(d), .wire1(q) );
    my_dff instance2 ( .clk(clk), .d(wire1), .wire2(q) );
    my_dff instance2 ( .clk(clk), .d(wire2), .q(q) );
    
endmodule
